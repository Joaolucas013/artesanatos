import { QuantidadeDirective } from './quantidade.directive';

describe('QuantidadeDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantidadeDirective();
    expect(directive).toBeTruthy();
  });
});
